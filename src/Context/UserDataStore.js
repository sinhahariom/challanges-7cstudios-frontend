import {createContext} from 'react';

//This is a BroadCast User Context to save the data at a global level. I have used Context API to 
//keep the design pattern clear and easier to understand.

export const UserDataStore = createContext({});           
